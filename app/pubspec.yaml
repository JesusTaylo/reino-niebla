name: reino_niebla
description: "Reino de Niebla - juego fitness de exploración con GPS."
publish_to: 'none'
version: 0.6.1+9

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.1.1
  latlong2: ^0.9.1
  geolocator: ^14.0.0
  http: ^1.2.2
  path_provider: ^2.1.4
  share_plus: ^11.0.0
  file_picker: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
